export interface Testimonial {
  id: number;
  name: string;
  image: string;
  text: string;
}

export interface FaqItem {
  question: string;
  answer: string;
}

export interface Deliverable {
  title: string;
  checked: boolean;
}